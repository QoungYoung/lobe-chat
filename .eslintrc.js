// const config = require('@lobehub/lint').eslint;
const config = {
  // 您的自定义规则和配置
  extends: [
      // 基础配置，例如 'eslint:recommended'
  ],
  rules: {
      // 您的自定义规则
  },
  // 其他配置选项
};
// config.extends.push('plugin:@next/next/recommended');
module.exports = config;
